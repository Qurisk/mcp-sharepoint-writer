{
  "name": "mcp-sharepoint-writer",
  "version": "1.0.0",
  "description": "MCP server (Streamable HTTP) qui permet à Claude.ai de déposer des fichiers dans SharePoint via Microsoft Graph API",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env index.js"
  },
  "dependencies": {
    "@azure/msal-node": "^2.16.2",
    "@modelcontextprotocol/sdk": "^1.13.1",
    "zod": "^3.24.4"
  },
  "engines": {
    "node": ">=20"
  }
}
